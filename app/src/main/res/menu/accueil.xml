<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/menu_carte"
        android:title="@string/carte_titre"
        android:icon="@android:drawable/ic_menu_mapmode"
        android:showAsAction="ifRoom" />
    <item android:id="@+id/menu_option"
        android:title="@string/accueil_menu_option"
        android:icon="@android:drawable/ic_menu_preferences"
        android:showAsAction="ifRoom" >
        <menu>
            <item android:id="@+id/menu_enregistrer"
                android:title="@string/accueil_menu_enregister"
                android:icon="@android:drawable/ic_menu_save"
                android:checkable="true"
                />
        </menu>
    </item>
</menu>
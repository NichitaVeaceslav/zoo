<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.gmail.veaceslav.nichita.monzoo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:targetApi="31">

        <activity
            android:name=".AccueilActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CarteActivity"
            android:label="@string/carte_titre"></activity>

        <activity
            android:name=".AquariumActivity"
            android:label="@string/aquarium_titre" />
        <activity
            android:name=".PopcornActivity"
            android:label="@string/popcorn_titre" />
        <activity
            android:name=".AlerteActivity"
            android:label="@string/alerte_titre" />

    </application>

</manifest>